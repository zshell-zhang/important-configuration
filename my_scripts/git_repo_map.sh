#!/bin.sh
# used to conveniently change directory to the target git repository 

map()
{
	if [ $1 -eq 0 ]; then # if the parameter 1 equals to 0
		
	elif [ $1 -eq 1 ]; then # if the parameter 1 equals to 1
		
}